plugins {
    id 'java'
    id 'com.gradleup.shadow' version '9.2.2'
}

apply plugin: 'com.gradleup.shadow'

group 'salad'
version '1.3.0-rc1'

repositories {
    google()
    mavenCentral()
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

shadowJar {
    archiveBaseName.set('salad')
    archiveVersion.set(project.version.toString())
    archiveClassifier.set(null)

    // Ensure reproducible builds
    reproducibleFileOrder = true
    preserveFileTimestamps = false

    // Minimize output but preserve critical dependencies
    minimize {
        exclude(dependency('org.seleniumhq.selenium:.*:.*'))
    }

    // Clean up unwanted files
    exclude '*.aut'
    exclude '*.yml'

    // Optional: Add metadata to manifest
    manifest {
        attributes(
                'Implementation-Title': 'Salad Automation Framework',
                'Implementation-Version': archiveVersion.get(),
                'Built-By': 'Aldo',
                'Created-By': "${System.properties['java.version']} (${System.properties['java.vendor']})"
        )
    }
}

dependencies {
    implementation 'io.appium:java-client:10.0.0'
    implementation 'org.seleniumhq.selenium:selenium-api:4.38.0'
    implementation 'org.seleniumhq.selenium:selenium-remote-driver:4.38.0'
    implementation 'org.seleniumhq.selenium:selenium-support:4.38.0'
    implementation 'net.datafaker:datafaker:2.5.3'
    implementation 'commons-io:commons-io:2.20.0'
    implementation 'org.apache.logging.log4j:log4j-core:2.25.2'
    implementation 'org.apache.logging.log4j:log4j-slf4j2-impl:2.25.2'
    implementation group: 'junit', name: 'junit', version: '4.13.2'
}